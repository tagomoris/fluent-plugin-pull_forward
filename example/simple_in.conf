<source>
  type pull_forward
  fetch_interval 10s
  timeout 10s
  <server>
    host localhost
    username moris
    password foobar
  </server>
   <server>
     host localhost
     username moris
     password fooba
   </server>
</source>

<match test.**>
  type stdout
</match>
