<source>
  type forward
</source>

<match test.**>
  type pull_forward
  buffer_path /tmp/pullforward.buffer.log
  self_hostname ${hostname}
  cert_auto_generate yes
  flush_interval 30s
  <user>
    username moris
    password foobar
  </user>
</match>
